# .jitpack.yml
jdk:
  - openjdk21

install:
  - mvn clean compile -DskipTests -B
  - mvn install -DskipTests -B


artifacts:
  - matlib-adaptor/target/*.jar
  - matlib-all/target/*.jar
  - matlib-common/target/*.jar
  - matlib-nms/target/*.jar
  - matlib-slimefun/target/*.jar
